{
    "servers": {
        "human-in-the-loop": {
            // Explicitly uses original-project branch for modular architecture + enhanced tool guidance
            // This ensures consistent behavior regardless of current working branch
            "command": "bash",
            "args": [
                "-c",
                "cd /home/lucas_galdino/repositories/mcp_servers/mcp-server-Human-In-the-Loop-MCP-Server && git checkout original-project && uv run python human_loop_server.py"
            ],
            "cwd": "/home/lucas_galdino/repositories/mcp_servers/mcp-server-Human-In-the-Loop-MCP-Server",
            "type": "stdio"
        },
        "deep-code-reasoning": {
            "command": "node",
            "args": [
                "/home/lucas_galdino/repositories/mcp_servers/mcp-server-deep-code-reasoning-mcp/dist/src/index.js"
            ],
            "cwd": "/home/lucas_galdino/repositories/mcp_servers/mcp-server-deep-code-reasoning-mcp",
            "env": {
                "GEMINI_API_KEY": "${input:geminiApiKey}"
            }
        }
    },
    "inputs": [
        {
            "type": "promptString",
            "id": "geminiApiKey",
            "description": "Enter your Gemini API Key",
            "password": true
        }
    ]
}